# michael-bootstrap local env template
#
# Copy to ~/.openclaw/workspace/.env.local on each machine if needed.
# Do not commit real values.

OPENAI_API_KEY=
GITHUB_TOKEN=
VERCEL_TOKEN=
SUPABASE_ACCESS_TOKEN=
GOOGLE_CLIENT_SECRET_PATH=

